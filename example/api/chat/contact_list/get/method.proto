syntax = "proto3";
package busrpc.api.chat.contact_list.get;

// Return contact list.
message MethodDesc {
  message Retval {
    // Users added to the contact list.
    repeated string usernames = 1;
  }
}
